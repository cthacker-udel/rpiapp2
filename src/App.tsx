/**
 * @file The landing page component, renders the `Dashboard`.
 */

import React from "react";

import { Dashboard } from "./modules/Dashboard";

/**
 * Renders the landing page component.
 * @returns The landing page component.
 */
const App = (): React.JSX.Element => <Dashboard />;

export default App;
